package com.interstellar.client;

public class DeletedExamples {
    /*
         shapeRenderer.begin(ShapeRenderer.ShapeType.Filled);
         shapeRenderer.setColor(Color.BLUE);
         shapeRenderer.rect(0,0,240,360);
         //Rechtecke
*/
    /*
    create:
      //Button Skin laden
        assetManager.load("System_Button_Skin.json", Skin.class);
        assetManager.finishLoading();
        systemButtonSkin = assetManager.get("System_Button_Skin.json", Skin.class);
     button = new ImageButton(systemButtonSkin);
        stage = new Stage(new ScreenViewport()); //Set up a stage for the ui
        stage.addActor(button); //Add the button to the stage to perform rendering and take input.

    render:
     stage.draw();
     button.setX(x);
     button.setY(y);
     */
    //.dispose = Löschen der Texture
}
